# COLE 任务倾向介入项目：v2 交接工作包

## 本版修正了什么

本版不再把“六类输出中最大概率很高”直接等同于“应该高介入”。五个明确类别表示玩家正在推进的中期子任务；`no_commitment` 表示当前没有足够证据认定玩家已承诺某一任务。只有明确任务概率高且稳定、环境证据一致、确实存在互补 Option，并且不会与玩家冲突时，才允许进入 Assist。

`unknown/invalid` 与 `no_commitment` 不同：前者表示数据或推理无效，不作为分类类别训练；后者是可以学习的行为语义，但不能直接触发 Assist。

## 五次正式交接

1. 环境组 → 数据组：`交接01_轨迹与任务事件接口_环境组到数据组.docx`
2. 环境组 → 联调展示组：`交接02_冻结COLE运行与动作覆盖接口_环境组到联调组.docx`
3. 数据组 → Agent 组：`交接03_任务级意图数据集_数据组到Agent组.docx`
4. Agent 组 → 联调展示组：`交接04_任务倾向模型与推理服务_Agent组到联调组.docx`
5. 联调展示组 → 项目统筹与汇报组：`交接05_任务条件介入控制器与演示发布_联调组到汇报组.docx`

每次交接必须由接收方在新终端现场验收，不能只看发送方截图或口头说明。

## AGENTS.md 使用方法

每个小组目录都有一份 `AGENTS.md`。

1. 若使用支持仓库指令的 AI 编码工具，把 `AGENTS.md` 放在本组工作目录根部，并连同代码一起打开。
2. 若使用普通对话式 AI，每次开始或恢复工作时，手动上传 `AGENTS.md`、对应 Word 指南、上一组 `HANDOFF_REPORT.md`、当前代码和最近一次完整日志。
3. 开场要求 AI 先判断当前阶段和缺失证据，本轮只推进一个验收门槛。
4. AI 不应只因脚本零退出就宣布完成；必须核对机器可读文件、统计、样例、日志或回放。

## 统一决策条件

```text
允许 Assist = 明确任务概率达到门槛
           + 任务连续稳定
           + 环境状态与任务一致
           + 存在互补 Option
           + 不与玩家争抢或冲突
           + no_commitment 不高
```

决策优先级：

```text
紧急最低限度动作
> 继续仍然有效的 current_option
> 重新选择 Assist / Support / Observe 与 Option
> 原始 COLE 动作
> 安全等待
```

## 权威资料

- COLE Platform：https://github.com/liyang619/COLE-Platform
- COLE 论文页：https://proceedings.mlr.press/v202/li23au.html
- COLE 论文 PDF：https://proceedings.mlr.press/v202/li23au/li23au.pdf
- Human-Aware RL：https://github.com/HumanCompatibleAI/human_aware_rl
- Overcooked-AI：https://github.com/HumanCompatibleAI/overcooked_ai

